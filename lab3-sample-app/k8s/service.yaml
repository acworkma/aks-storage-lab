apiVersion: v1
kind: Service
metadata:
  name: aks-storage-app-service
  labels:
    app: aks-storage-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: aks-storage-app
